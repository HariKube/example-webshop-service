---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: payments.product.webshop.harikube.info
spec:
  group: product.webshop.harikube.info
  names:
    kind: Payment
    listKind: PaymentList
    plural: payments
    singular: payment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ownerRef.name
      name: Owner
      type: string
    - jsonPath: .spec.price
      name: Price
      type: string
    - jsonPath: .status.paymentTimestamp
      name: PaymentTime
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Payment is the Schema for the payments API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PaymentSpec defines the desired state of Payment.
            properties:
              orderRef:
                description: OrderRef represents the owner reference of the
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  blockOwnerDeletion:
                    description: |-
                      If true, AND if the owner has the "foregroundDeletion" finalizer, then
                      the owner cannot be deleted from the key-value store until this
                      reference is removed.
                      See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                      for how the garbage collector interacts with this field and enforces the foreground deletion.
                      Defaults to false.
                      To set this field, a user needs "delete" permission of the owner,
                      otherwise 422 (Unprocessable Entity) will be returned.
                    type: boolean
                  controller:
                    description: If true, this reference points to the managing controller.
                    type: boolean
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
                    type: string
                required:
                - apiVersion
                - kind
                - name
                - uid
                type: object
                x-kubernetes-map-type: atomic
              price:
                description: Price represents the price of the payment in cents.
                format: int64
                type: integer
            required:
            - orderRef
            - price
            type: object
          status:
            description: PaymentStatus defines the observed state of Payment.
            properties:
              errorMessage:
                type: string
              errorTimestamp:
                format: date-time
                type: string
              paymentTimestamp:
                format: date-time
                type: string
            type: object
        type: object
    selectableFields:
    - jsonPath: .spec.ownerRef.name
    served: true
    storage: true
    subresources:
      status: {}
