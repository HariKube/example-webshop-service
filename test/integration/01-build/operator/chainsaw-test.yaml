apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: operator
spec:
  namespace: default
  skipDelete: true
  timeouts:
    exec: 5m
  steps:
  - name: Build operator
    try:
    - script:
        content: cp ../../../../config/config/email-smtp-secret-sample.yaml ../../../../config/config/email-smtp-secret.yaml || true
    - script:
        content: make -C ../../../.. package docker-build docker-load
