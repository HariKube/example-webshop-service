apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: operator
spec:
  namespace: default
  skipDelete: true
  timeouts:
    exec: 5m
  steps:
  - name: Build operator
    try:
    - script:
        content: TAG=$(date +"%s") make -C ../../../.. package docker-build docker-load
